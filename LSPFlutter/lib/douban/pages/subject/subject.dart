import 'package:flutter/material.dart';

class LSPSubjectPage extends StatelessWidget {
  const LSPSubjectPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
